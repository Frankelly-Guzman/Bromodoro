import Timer from "../components/bromo/Timer"

const Main = () => {
  return (
    <div className="bg-black flex items-center justify-center min-w-screen min-h-screen">
      <Timer />
    </div>
  );
};

export default Main;
